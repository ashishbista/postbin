%p#notice= notice
- if @posts.empty?
  %h3 No requets receieved yet
  %p Run
  %pre
    curl -X POST -d "foo=bar" #{url}
- else
  %table.table.table-hover.table-responsible
    %thead
      %tr
        %th.col-md-6 Header
        %th.col-md-6 Body
        %th{:colspan => "3"}
    %tbody
      - @posts.each do |post|
        %tr
          %td= post.header
          %td= post.body
          %td= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }
%br/
